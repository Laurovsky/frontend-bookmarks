# Это тестовый блок кода

## Это заголовок

```js
const numbers = [5, 10, 3, 20];

const max = numbers.reduce((acc, num) => {
  return num > acc ? num : acc;
}, numbers[0]);

console.log(max); // 20
```

пукич **какич**

функция `setTimeout()` принимает `callback`

1. первый
2. второй
3. третий